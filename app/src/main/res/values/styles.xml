<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay.v14.Material</item>
    </style>

    <style name="AppTheme.ToolbarStyle" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <style name="AppTheme.LastSearchStyle">
        <item name="android:background">@drawable/last_search_border</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textStyle">bold</item>
        <item name="android:gravity">center</item>
        <item name="android:selectAllOnFocus">true</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="AppTheme.LastSearchTitleStyle">
        <item name="android:gravity">center_horizontal</item>
        <item name="android:textSize">18sp</item>
        <item name="android:text">@string/last_fetched_url</item>
    </style>

    <style name="AppTheme.CheckedTextViewStyle">
        <item name="android:background">@drawable/selector</item>
        <item name="android:checked">false</item>
        <item name="android:drawableStart">@drawable/ic_music_note_black_24dp</item>
        <item name="android:drawableEnd">?android:attr/listChoiceIndicatorMultiple</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textAppearance">?android:textAppearanceMedium</item>
        <item name="android:layout_marginEnd">8dp</item>
    </style>

    <style name="AppTheme.SelectAllCheckedTextViewStyle" parent="AppTheme.CheckedTextViewStyle">
        <item name="android:textColor">#22928E</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
